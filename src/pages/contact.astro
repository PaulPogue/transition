---
import Layout from '../layouts/Layout.astro';
import ScheduleButtonLarge from '../components/buttons/ScheduleButtonLarge.astro';
import BroadStreet from '../assets/images/BroadStreet.png';
import ContactForm from '../components/ContactForm';
import { FaLocationDot } from 'react-icons/fa6';
import { FaPhone } from 'react-icons/fa';
import { FaFax } from 'react-icons/fa';
---

<Layout>
  <!-- Contact Section -->
  <section class="bg-gradient relative w-full py-24">
    <!-- Background layers (desktop only) -->
    <!-- <div class="absolute inset-0 hidden md:block z-0 pointer-events-none"> -->
    <!-- Background image -->
    <!-- <div
        class="absolute inset-0 bg-cover bg-center"
        style={`background-image: url('${BroadStreet.src}');`}
      >
      </div> -->

    <!-- Brand color wash -->
    <!-- <div class="absolute inset-0 bg-[var(--firmDarkBlue)]/50"></div> -->

    <!-- Gradient for legibility -->
    <!-- <div
        class="absolute inset-0 bg-gradient-to-b from-transparent to-black/30"
      >
      </div> -->

    <!-- Photo credit -->
    <!-- <div
        class="absolute bottom-4 left-6 text-[11px] tracking-widest uppercase
               text-white/50 select-none"
      >
        Broad Street • Central City, KY • CIRCA 1980
      </div> -->
    <!-- </div> -->

    <!-- Content -->
    <div class="relative z-10 px-4 sm:px-10 text-white">
      <div class="max-w-3xl mx-auto flex justify-center">
        <div class="w-full">
          <ContactForm client:load />
        </div>
      </div>
    </div>
  </section>
</Layout>
